<?xml version="1.0" encoding= "UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="co.kr.doje.mapper.CommentMapper">
	<select id="getCommentList" resultType="hashmap" parameterType="int">
		SELECT comment.* , user.username 'writer_name' FROM comment JOIN user
		ON comment.writer = user.id 
		WHERE board_id = #{board_id}
		ORDER BY gr DESC , seq ASC;
	</select>
	
	<update id="updateComment" parameterType="hashmap">
		UPDATE comment SET seq = seq + 1 WHERE gr = #{group} AND seq <![CDATA[ > ]]> #{seq} and board_id = #{board_id}
	</update>
	
	<insert id="insertComment" parameterType="hashmap">
		INSERT INTO comment(board_id , content , writer , level , gr , seq) VALUES(#{board_id} , #{content} , 1 , #{level}+1 , #{group} , #{seq}) 
	</insert>
</mapper>