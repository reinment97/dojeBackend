<?xml version="1.0" encoding= "UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
 
<mapper namespace="co.kr.doje.mapper.UserMapper">
 	<select id="getLogin" resultType="co.kr.doje.VO.UserVO" parameterType="hashmap">
 		SELECT * FROM user WHERE useremail = #{useremail} AND userpassword = #{userpassword};
 	</select>
 	<insert id="insertUser" parameterType="hashmap">
 		INSERT INTO user( username , useremail , userpassword ) VALUES ( #{username} , #{useremail} , #{userpassword} )
 	</insert>
</mapper>